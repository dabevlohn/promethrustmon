[package]
name = "for-metrics"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
lazy_static = "1.5.0"
prometheus = { version = "0.14.0", features = ["process"] }
tokio = { version = "1.50.0", features = ["full"] }
